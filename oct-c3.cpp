#include <bits/stdc++.h>
using namespace std;

int main()
{   
    long long int count, N, test, i, j, per, M;
        cin >> test;
    while(test--){
        cin >> N >> M;
        long long int Arr[N], Brr[N], Cnd[N];
        per = M/N;
        j = N;
        while(N--)
            cin >> Arr[i];
        N=j; i=0;
        while(N--)
            cin >> Brr[i];
        N=j; i=0;
        while(N--)
            cin >> Cnd[i];


        }
    return 0; 
}