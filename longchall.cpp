#include<bits/stdc++.h>
using namespace std ;
 
main()
{
	ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    sort(arr, arr + n);
    /*
    int i=0, j = i+1;
    for( i; i<n; i++){
        num = 2*arr[j] - arr[i];

        for(k=j+1; k++;){
            if(arr[k] == num){
                include(i,k);
            } else if( arr[k] > num){
                k--;            
                break();
                j++;
                }
            
        }
    }
    */

}